# [CPMToolkit API Reference](@id cpmtoolkit-api)

Full docstring index for the `CPMToolkit` package.
Note that `CPMToolkit` re-exports CoreCPM's public API; the symbols listed here are those
*defined* in CPMToolkit itself. For CoreCPM types and functions see the
[CoreCPM API Reference](@ref corecpm-api).

---

```@autodocs
Modules = [CPMToolkit, CPMToolkit.System, CPMToolkit.Problem]
Order   = [:type, :function]
```
