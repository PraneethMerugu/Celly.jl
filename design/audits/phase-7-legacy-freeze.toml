version = 2
status = "closed"
closed_by = "phase-13-api-freeze"

# Phase 7 froze the historical engine while its scientific replacement was built. Phase 13
# closes that quarantine: no frozen implementation, mixed production signature, or executable
# consumer remains. The empty inventories are intentional and are enforced by
# `scripts/check_legacy_containment.jl`.
[files]

[signatures]

[consumer_signatures]

[closure]
historical_engine = "absent"
legacy_benchmark_path = "absent"
legacy_documentation_consumers = "absent"
neural_satellite_legacy_implementation = "removed-pending-phase-14-or-later-redesign"
mermaid_legacy_coupling = "removed-pending-post-phase-13-redesign"
toolkit_legacy_compiler = "absent"
